package frogmodaiGame;

import java.util.HashMap;
import java.util.Map;
import java.util.stream.Collectors;

public final class CommandFactory {
	/*private final Map<String, FFCommand> commands;
	
	private FFCommandFactory() {
		commands = new HashMap<String, FFCommand>();
	}
	
	public void addCommand(final String name, final FFCommand command) {
		commands.put(name, command);
	}
	
	public void executeCommand(String name, Object input) {
		if (commands.containsKey(name)) {
			commands.get(name).execute(input);
		}
	}
	
	public static FFCommandFactory init() {
		final FFCommandFactory cf = new FFCommandFactory();
		
		//cf.addCommand("test", () -> System.out.println("eh"));
		
		return cf;
	}*/
}
